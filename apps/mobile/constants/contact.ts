/**
 * Who is responsible for a published build, and how to reach them.
 *
 * Empty by default, and the app hides the corresponding sections when they are. This
 * repository is source, not a distribution: whoever builds and hands the app to other
 * people is the responsible party, not whoever wrote the code.
 *
 * Fill these in before distributing a build. In Germany a published app generally needs
 * an Impressum under §5 DDG; a build you compile for yourself does not.
 */

/** Name of the person or organisation distributing the build. */
export const MAINTAINER = '';

/** Address for questions and feedback. Shown in the imprint and under Mehr. */
export const CONTACT_EMAIL = '';
